#include <stdio.h>

int main() {
    char domestic_z[4][10];  
    char wild_p[4][10];

    printf("Enter the domestic animals:\n");
    for(int x = 0; x < 4; x++) {
        scanf("%s",& domestic_z[x]);  
    }

    printf("Enter the wild animals:\n");
    for(int s = 0; s < 4; s++) {
        scanf("%s",&wiild_p[s]);  
    }

    printf("list of all animals:\n");
    for(int i = 0; i < 4; i++) {
        printf("%s\t", domestic_z[i]); 
printf("\t");
        printf("%s\n", wild_p[i]);  
    }
printf("...END OF PROGRAM...");
    return 0;
}
